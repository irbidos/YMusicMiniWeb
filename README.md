<h1>🎵 Яндекс.Музыка мини</h1>
  <p><strong>Неофициальный компактный десктоп-клиент для Яндекс.Музыки</strong></p>
  <p>
    <a href="#-особенности">Особенности</a> •
    <a href="#-запуск">Запуск</a> •
    <a href="#-сборка">Сборка</a> •
    <a href="#-лицензия">Лицензия</a>
  </p>

---

**Яндекс.Музыка мини** — лёгкий клиент Яндекс.Музыки: официальный сайт открывается в WebView2, а рядом с ним живёт мини-пульт для управления воспроизведением. Пульт всегда под рукой и не перекрывает рабочий стол.

---

## ✨ Особенности

- **Официальный сайт** — вся Яндекс.Музыка как в браузере: «Моя волна», плейлисты, поиск, свой профиль, подписка.
- **Мини-пульт** — компактное frameless-окно с обложкой, названием трека, кнопками «Назад / Пауза-Плей / Вперёд» и слайдером громкости.
- **Живое состояние** — обложка и метаданные трека синхронизируются с плеером автоматически.
- **Управление без показа окна** — переключение треков работает даже если окно музыки свёрнуто или скрыто.
- **Поверх всех окон** — кнопка «Закрепить» держит пульт на переднем плане.
- **Залипание к краям экрана** — перетащите пульт к краю или к панели задач — он прилипнет вплотную.
- **Один процесс** — ничего лишнего в трее и в автозагрузке.

---

## 🚀 Запуск

### Требования

- Windows 10/11 64-bit (WebView2 Runtime установлен вместе с системой)
- Python 3.9+

### Из исходников

```bash
git clone https://github.com/frizzy/yandex-music-mini.git
cd yandex-music-mini

python -m venv venv
venv\Scripts\activate          # Windows
# source venv/bin/activate    # Linux/macOS

pip install -r requirements.txt

python run_yandex_web.py
```

## 📦 Сборка

```bash
pip install pyinstaller
pyinstaller YMusicMiniWeb.spec
```

Готовый один EXE появится в `dist\YMusicMiniWeb.exe`. Запускай его откуда угодно — всё внутри.

> При первом запуске EXE распаковывает DLL во временную папку (`%TEMP%\_MEI...`), поэтому старт занимает ~1–2 сек.

---

## 🔧 Как это работает

Приложение поднимает два окна pywebview на движке WebView2:

1. **Окно музыки** — `https://music.yandex.ru/` (скрывается автоматически при запуске);
2. **Окно пульта** — frameless-окно с HTML-интерфейсом поверх.

Управление воспроизведением опирается на надёжные пути вместо синтетического JS:

- **Реальный Win32-клик** — в DOM ищется кнопка по `aria-label` (`Воспроизведение`, `Следующая песня` и т.д.), берётся её центр, и в `Chrome_RenderWidgetHostHWND` отправляется настоящее `WM_LBUTTONDOWN`/`WM_LBUTTONUP` через `PostMessage`. Браузер считает клик доверенным (`isTrusted=true`), поэтому autoplay-политика не блокирует воспроизведение.
- **WM_APPCOMMAND** — запасной путь: сообщение `APPCOMMAND_MEDIA_*` отправляется напрямую в дочернее WebView2-окно, минуя глобальные медиа-кнопки, и не задевает другие плееры (YouTube, Spotify и т.п.).
- **Space + фокус** — крайний случай, если первые два пути не сработали.

`media_session` страницы используется для чтения метаданных и состояния воспроизведения.

**Громкость** управляется через Windows Core Audio API (`pycaw`) — напрямую на уровне аудио-сессии процесса WebView2 в микшере Windows. Не зависит от DOM браузера и работает гарантированно.

---

## ⚠️ Про OAuth и токены

В этой версии вход выполняется штатно на сайте music.yandex.ru в WebView2: пароль вводится только на странице Яндекса, сессия живёт в профиле WebView2. Никакой ручной OAuth-схемы и хранения токенов в файлах не требуется.

---

## 📄 Лицензия

Смотрите файл [LICENSE](LICENSE). Проект неофициальный и не связан с Яндексом.